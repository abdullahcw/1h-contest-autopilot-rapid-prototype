import { SignedUrlDirective } from './signed-url.directive';

describe('SignedUrlDirective', () => {
  it('should create an instance', () => {
    const directive = new SignedUrlDirective();
    expect(directive).toBeTruthy();
  });
});
