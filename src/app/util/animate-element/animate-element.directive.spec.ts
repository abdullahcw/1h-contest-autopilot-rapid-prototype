import { AnimateElementDirective } from './animate-element.directive';

describe('AnimateElementDirective', () => {
  it('should create an instance', () => {
    // const directive = new AnimateElementDirective();
    // expect(directive).toBeTruthy();
  });
});
