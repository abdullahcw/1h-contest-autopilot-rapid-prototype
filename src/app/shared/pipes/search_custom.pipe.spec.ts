import { SearchCustomPipe } from './search_custom.pipe';

describe('SearchCustomPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchCustomPipe();
    expect(pipe).toBeTruthy();
  });
});
